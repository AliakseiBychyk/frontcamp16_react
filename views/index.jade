extends layout

block content
  .container
    h1= title
    p.lead Welcome to #{title}

    if (!user)
      a(href="/login") Login
      br
      a(href="/register") Register
      br
      a(href="/user") Find user
    if (user)
      p You are currently logged in as #{user.username}
      a(href="/logout") Logout
